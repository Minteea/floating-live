{
  "name": "floating-living",
  "version": "0.6.6",
  "type": "commonjs",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "Minteea",
  "license": "GPLv3",
  "scripts": {
    "tsc": "tsc -b",
    "clear": "rimraf index.js index.d.ts src/**/*.js src/**/*.d.ts tools/**/*.js tools/**/*.d.ts",
    "test": "npm run clear && ts-node demo/demo.ts",
    "build-test": "npm run build && node demo/demo.js",
    "build": "npm run clear && npm run tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "acfun-live-danmaku": "^0.6.1",
    "axios": "^0.26.0",
    "bilibili-live-ws": "^6.0.3",
    "ws": "^8.5.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  }
}
